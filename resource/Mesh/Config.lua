-- all file paths are relative to current work directory

--package.path = package.path .. "../resource/Mesh/?.lua"

return
   {
      ResRoot = "../resource/Mesh/",
      -- BaseFiles =
      -- 	 {
      -- 	 },
      DefaultRes = "../resource/Mesh/Default.lua"
   }
